angular.module('MyChart.parada')

/**
 * 
 */
.controller('ParadaController', [ '$rootScope', '$scope', '$log', function($rootScope, $scope, $log) {
	'use strict';

	$rootScope.titulo = 'Parada';
} ]);